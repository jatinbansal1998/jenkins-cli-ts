{
  "name": "jenkins-cli-ts",
  "version": "0.8.13",
  "type": "module",
  "private": true,
  "sideEffects": false,
  "module": "src/index.ts",
  "bin": {
    "jenkins-cli": "dist/jenkins-cli"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun scripts/build-local.ts",
    "install:global": "bun run build && bun link",
    "lint": "oxlint --deny-warnings --type-aware && bun scripts/lint-guards.ts",
    "lint:fix": "oxlint --fix --type-aware",
    "knip": "knip",
    "typecheck": "tsc --noEmit",
    "check": "bun run lint:fix && bun run format && bun run lint && bun run typecheck && bun run knip",
    "test": "bun test",
    "sentry:smoke": "bun scripts/verify-sentry.ts",
    "sentry:smoke:build": "bun scripts/build-sentry-smoke.ts",
    "test:integration:jenkins": "bun scripts/run-with-linux-keyring.ts -- bun scripts/test-jenkins-integration.ts",
    "test:mutation": "bun scripts/test-mutation.ts",
    "test:mutation:jenkins": "bun scripts/run-with-linux-keyring.ts -- bun scripts/test-jenkins-integration.ts --mutation",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@sentry/cli": "3.6.2",
    "@types/bun": "1.3.14",
    "@types/yargs": "^17.0.35",
    "husky": "^9.1.7",
    "knip": "^6.32.0",
    "lint-staged": "^17.0.8",
    "oxlint": "^1.73.0",
    "oxlint-tsgolint": "^7.0.2001",
    "prettier": "3.9.4",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@clack/core": "1.4.3",
    "@clack/prompts": "^1.7.0",
    "@sentry/bun": "^10.67.0",
    "cross-keychain": "^1.1.0",
    "yargs": "^18.0.0",
    "yargs-parser": "^22.0.0"
  },
  "lint-staged": {
    "**/*.{ts,tsx,js,jsx,json,css,md}": [
      "oxlint --fix",
      "prettier --write"
    ]
  }
}
